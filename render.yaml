services:
  - type: web
    name: ecg-smart-door-lock
    env: python
    pythonVersion: "3.11.0"        
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 8000
    envVars:
      - key: DEBUG
        value: false